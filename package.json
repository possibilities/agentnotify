{
  "name": "agentnotify",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Send and browse desktop notifications from agents and scripts",
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": "1.3.14"
  },
  "bin": {
    "agentnotify": "src/cli.ts"
  },
  "scripts": {
    "start": "bun run src/cli.ts",
    "lint": "biome check .",
    "format": "biome format --write src test scripts",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "check": "bun run lint && bun run typecheck && bun run test"
  },
  "dependencies": {
    "node-notifier-cli": "^1.0.2",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.0",
    "@types/bun": "latest",
    "typescript": "5.9.3"
  }
}
