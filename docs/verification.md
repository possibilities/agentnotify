# Verification

Verified on macOS 26.5.2, arm64, Swift 6.3.3 Command Line Tools, September 8, 2026.

- `swift run NotifyCoreChecks`: 17 scenarios covering persistence, response races, idempotency, optimistic concurrency, timeout/snooze, change cursors, literal replies, validation, durable attachments, and bounded history pages.
- `python3 scripts/test-integration.py`: real CLI, private Unix socket, and stdio MCP. Covers concurrent prompts, exact stdout/exit codes, pipe input, schedule validation, removal, caller interruption including SIGKILL, callback execution once, restart durability, MCP discovery/calls, and invalid input.
- AgentStart’s contract validator accepts `agentnotify guide --json` as fleet contract v1.
- The debug native panel check opens the actual popover, detaches into a visible NSPanel while preserving the hosting controller, persists read state, reattaches, and closes. The preview uses a temporary store and its own bundle identity; it exits after verification.
- The production SwiftUI view was rendered and inspected in light/dark, detached/expanded, and narrow empty-search states. The actual detached-window render was also inspected. [Light fixture preview](screenshots/light-inbox.png) and [dark fixture preview](screenshots/dark-inbox.png) are generated by debug `render-previews`.
- `python3 scripts/test-install.py`: temporary-destination installation, foreign-command preservation, terminal-notifier alias, running-app refusal without stopping it, idempotent reinstall, and exact deployed-SHA receipt all passed. Release bundle signature verification passed.
- The isolated AgentStart integration passes its complete `tests/validate.sh` suite. Project document checks and shell checks pass.

The initial Computer Use session timed out selecting the preview app; September 9 sessions below successfully exercised it. System banner visibility, real Notification Center button clicks, reply input, sound, Focus, sleep/reboot, and distribution signing/notarization are not established by these tests. The app’s macOS authorization query was observed working; the preview did not change notification permission.

## Repeat native verification

September 9 focus refinement: Computer Use successfully inspected an isolated preview bundle. Opening the popover and detaching left focus on the window, Tab focused Inbox with its native indicator, and a pointer click cleared that control focus. Mouse category selection remained quiet. Search accepted typed text, and Escape closed search. The installed inbox was not used for these checks. Debug and release builds passed.

September 9 pinning fix: with auto-hide enabled, the actual status window was observed above the display with no screen. Four native pin/unpin cycles now retain the same 440 × 610 content rectangle at the tray, with a normal popover pointer. After removing auto-hide tracking, the debug check verifies that both real window types stay still across revealed/contracted anchor geometry and repeated show requests. It also checks negative-coordinate and notched-screen projection. Computer Use verified normal unpin rendering, visibility after activating Finder, header dragging by 60 × 120 points, Tab focus, and quiet focus after pinning. Tests use a disposable bundle and empty store. Multiple physical displays, notched hardware, and full-screen Spaces remain unverified.

Build the debug product and package it into a disposable app bundle with its own bundle ID. Set `AGENTNOTIFY_STATE_DIR` to a seeded disposable store, `AGENTNOTIFY_PREVIEW=1`, and `AGENTNOTIFY_SELF_CHECK_DIR` to an output directory. Launch the bundle with `app`. The debug check emits `native-panel-check.json` and `native-detached.png`, then terminates. This instrumentation is omitted from release builds.

September 9 hover dismissal: native checks cover delayed dismissal, cancelling on re-entry, opening with the pointer elsewhere, pausing for menus, keyboard/protected interactions, and cancelling pending work when stopped. Computer Use verified that the unpinned preview dismisses after pointer exit, survives a brief exit and return, and stays open with search text focused or keyboard navigation active. Pinning returns to a persistent panel. All checks used the disposable empty inbox; menu and VoiceOver guards were checked through the dismissal policy, not a full assistive-technology session.

September 9 outline and placement refinement: inspected the native popover contour and matched its continuous 20-point corner in a transparent, borderless panel. Computer Use compared both outlines, resized the pinned panel from 440 to 500 points, dragged it, and confirmed unpinning retained the same window with no triangle. Hover exit dismissed the unpinned floating panel. Native checks also verify three pin/unpin cycles at a manual position and restoration of the anchored popover on a fresh opening. The earlier position, focus, and hover checks still pass.

September 9 drag jitter fix: reproduced an 80 × 90-point extra movement by repeating a queued event after its window moved. Header dragging now uses the event's global Quartz position with the captured starting origin, accounting for the opposite AppKit Y axis. Native regression checks exercise the actual header view with repeated events, direction reversals, return to origin, mouse-up, and a second grab; all existing placement and hover checks pass. Computer Use verified diagonal drags in opposite directions, retained triangle-free placement on unpin, resizing from 440 to 500 points, and dragging after that layout change. The automation delivers event positions without moving `NSEvent.mouseLocation`, so event coordinates also make this path verifiable without depending on the live pointer. Tests used an isolated empty inbox; sustained physical dragging across displays remains a manual check.

September 9 wider drag coverage: added drag surfaces behind the panel content and inside the list, filling the viewport even for a single notification. Computer Use verified empty-inbox and sparse-list background dragging, top and side margin gestures, preserved manual placement on unpin, toolbar clicks, search and reply typing, and native edge resizing. Wheel scrolling over the list’s blank left margin moved an overflowing nine-item fixture from top to bottom. The existing native position, drag-coordinate, and hover checks passed against a separate empty store. All fixture notifications remained isolated from the installed inbox.

September 9 arrival previews: a four-agent design/implementation/review team compared three actual native renderers in single, burst, and long-content scenarios, in light and dark (18 renders). The selected 440 × 126 preview matches the full inbox body, including the native 13-point screen-edge inset. Debug checks cover nonactivation, keyboard focus, hover pause and stable identity, coalesced bursts, expiry, stale timers, group replacement, withdrawal, and opening without a presenter-side durable mutation. The existing pin, manual-placement, drag, and hover checks remain included. Durable tracker and visual-owner policy coverage brings the core suite to 28 checks; the final run passed 316 CLI/socket/MCP checks.

Computer Use inspected the compact surface in an isolated signed bundle, clicked into the full inbox, and verified the selected item became read while remaining active, with no response or callback effect. With the inbox pinned open, a new notification produced an in-panel cue; reading the row cleared that cue while the count stayed at two waiting tasks. A bounded debug-only 60-second inspection hold allowed physical click-through; normal expiry is verified separately by the native lifecycle check. The studio's expansion uses matching synthetic content and queue counts without touching the real inbox. Actual VoiceOver navigation, Focus, system-banner/sound delivery, and multiple physical displays remain outside this pass.

Run `.build/debug/agentnotify arrival-studio` for the separate transient comparison controller, or `render-arrivals <directory>` for native PNGs. Release builds exclude both commands, the alternate renderers, and `AGENTNOTIFY_ARRIVAL_PREVIEW_HOLD`. [Light arrival](screenshots/light-arrival.png) and [dark arrival](screenshots/dark-arrival.png) show the production renderer.

September 9 studio interaction repair: disabled content-driven hosting sizing and supplied explicit native view bounds after a clipped synthetic inbox was observed. Computer Use verified a fully rendered 440 × 610 inbox in both appearances; all three design/scenario selectors; matching scenario counts of 3, 8, and 12; completion and Undo; pin state; search; and the settings explanation. A timed arrival reached the dismissed state, and opening the inbox during playback survived the obsolete dismissal deadline. Rebuilt all 18 native comparison renders and inspected Compact Toast/Burst/Light and Queue Shelf/Long/Dark. Debug and release builds passed; the release binary contains none of the studio command, control, or alternative markers. This pass does not establish physical hover timing, multi-display arrangement, VoiceOver operation, or system notification delivery.

## Arrival preferences and notification settings

September 9: 33 core checks and 360 CLI/socket/MCP checks passed, including preference persistence, concurrent revisions, request replay, no-op writes, invalid choices, headless presentation refusal, and isolation from the notification change feed. Debug native arrival checks pass for all three production renderers: stable hovered/pressed identity, deferred style adoption, panel dimensions, no keyboard activation, expiry, and no durable task mutation. The injected settings observer checks activation, wake, visible-only polling, hidden-window cancellation, restart, and cleanup without changing macOS permissions.

Computer Use verified the actual Preferences picker, immediate GUI-to-CLI and CLI-to-GUI preference updates, the native completion checkbox, named Undo completion restoring its item, and Hide confirmation leaving it completed. The latest native build also verified the category menu’s 8-point chevron gap, category selection, the single “Turn on macOS banners” prompt, and opening Preferences from More. Production view renders cover all three preference previews and completion feedback in both appearances. No automated test uses the real inbox. An actual macOS banner-style toggle during an open window remains a manual check.

The complete panel self-check currently fails on this desktop’s geometry when an unpin projects content from (2048, 1479, 440, 610) to (1834, 816, 440, 610). The same failure was reproduced with the unchanged 15feffe baseline executable. This task does not change pin/drag placement; the standalone native arrival and settings checks isolate the changed behaviors and pass.

AgentStart’s six MCP-install tests and complete validation suite passed. The tests cover one-time AgentNotify adoption in existing fleet/Grok toolsets, stable credentials, retained explicit restrictions, and untouched custom sets. Project-document checks report no errors and one existing advisory about the stable-anchor ADR’s replacement wording.

Shim setup: Computer Use verified the one-time offer, Not now, opening Preferences afterward without another offer, and Install shim becoming Reinstall shim after successful installation into the disposable service’s `shim-home`. Core checks cover durable offer acknowledgment, unavailable installers, delegation, and executable/marker detection. CLI and MCP observe the same isolated installed state. The owner’s router tests preserve original fallback, literal input, exit status, signals, refusal of foreign commands, and no retry after submission.

September 9 hover-dismissal follow-up: the standalone debug `check-hover` passes nine checks covering the longer unvisited-opening grace period, pointer-exit grace, re-entry, menus, protected editing, pointer exit after keyboard use, a missing exit followed by continued outside movement, sheet completion, and stopping/pinning cleanup. It uses a hidden native window and injected hit testing, so the operator’s physical pointer cannot change the deterministic checks. The disposable native app was also opened and unpinned through Computer Use; the regression suite provides the bounded timing proof. No notification lifecycle, pin position, or menu-bar geometry changed.
